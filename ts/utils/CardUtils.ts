import WikiManager from '../managers/WikiManager';
import { ICard } from '../interfaces/ICard';
import { ICardInfo } from '../interfaces/ICardInfo';

export class CardUtils {
    public static parseWikiCard(url: string) {}
}
